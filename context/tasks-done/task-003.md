Есть баг в заголовках, например:
### Описание подхода к решению

что когда у нас нумерация становится двузначной=трехзначной и так далее получается так, что отступ между номером и заголовком теряется, так как появляется новый символ номера

и получается что-то вроде

1.2.10Описание подхода к решению

а должно быть с отступом:

1.2.10 Описание подхода к решению

**Выполнено:** Добавлен ведущий пробел для нумерованных заголовков в `renderable_factory.py` (при создании Heading с `numbered=True` перед вызовом `_create_runs` вызывается `heading.add_run(" ")`). Тесты: `tests/test_heading.py` — `TestHeadingNumberingSpace`.
