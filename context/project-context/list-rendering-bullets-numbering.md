# Рендер списков: буллеты и нумерация

- **Файл**: `md2gost/renderable/list.py` — класс `List`, метод `add_item()`.
- **Неупорядоченные списки**: маркер — длинное тире `—` (Unicode U+2014).
- **Упорядоченные списки**: маркер вида `"1."`, `"2."` и т.д.
